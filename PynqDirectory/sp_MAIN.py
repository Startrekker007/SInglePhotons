from Single_Photons.SP import SP_TOOLS
from time import sleep
SPT = SP_TOOLS()
#while(1):
    # Counter test
    # SPT.pc_set_window(1.0,0xF)
    # SPT.pc_enable_channels(0xF)
    # print("Waiting...")
    # SPT.pc_wait_for_rdy(1)
    # print(SPT.pc_read_counts(0))
    # SPT.pc_disable_channels(0xF)
    # Activate pulse generator
    # SPT.pg_set_enabled(0)
    # SPT.pg_set_channel_freq(0,20000)
    # SPT.pg_set_enabled(1)
    # SPT.pg_set_channel_freq(1,20000)
    # SPT.pg_set_delay(1,0.000010)
    # sleep(2)
    # Begin reading the pulse delay with CT to prove concurrent operation
    # while(1):
    #     print("Arming")
    #     print(SPT.ct_arm_and_wait()*1000000000)
    #     sleep(0.5)



